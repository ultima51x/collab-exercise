.container
  .row
    .col
      %h2 Videos
      %div
        Use Refresh Buttons to load videos from an external API.

  = form_with url: videos_path, method: :get do |form|
    .row
      .col
      .col-3.p-0.mx-1
        = form.text_field :q, class: 'form-control', value: params[:q]
      .col-auto.p-0.mx-1
        = form.submit "Search", class: 'btn btn-secondary form-control'
      .col-auto.p-0.mx-1
        %div
          %a.btn.btn-warning.form-control{ href: refresh_videos_path, data: { turbo_method: 'put' }} Refresh Videos

  - @videos.each do |video|
    = render 'video', video: video
